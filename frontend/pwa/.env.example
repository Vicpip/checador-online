# Local dev: point straight at uvicorn.
# Behind nginx (docker-compose): use /api instead.
VITE_API_URL=http://localhost:8000

# Client-side photo compression — mirror the backend's FOTO_MAX_WIDTH / FOTO_QUALITY.
VITE_FOTO_MAX_WIDTH=1200
VITE_FOTO_QUALITY=0.75
